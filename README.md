# GHOSTDAG-parameter-explorer
GHOSTDAG Parameter Explorer is a zero-dependency CLI tool for exploring consensus mechanics. Without requiring a global Python install, it lets users interactively compute and visualize relationships between block rates ($\lambda$), network propagation delay ($D$), anticone sizes ($x$), and $k$ bounds.
